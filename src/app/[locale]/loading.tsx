import type {} from 'ldrs';
function Loading() {
    return (
        <div className={'flex items-center justify-center pb-14 min-h-[calc(100vh-59px)]'}>
            <l-tail-chase size="80" speed="1.75" color="currentColor"></l-tail-chase>{' '}
        </div>
    );
}

export default Loading;
