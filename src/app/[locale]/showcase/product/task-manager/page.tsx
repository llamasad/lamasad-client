import MainPage from '@/components/products/task-manager/main-page';

function Product() {
    return (
        <div>
            <MainPage hasMacWrap={false} />
        </div>
    );
}

export default Product;
